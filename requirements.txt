# Created: 18:59 21-Apr-2026
# Updated: 21:31 21-Apr-2026
# Updated: 21:55 27-Apr-2026
# Cross-platform runtime deps. Plain `uvicorn` (no [standard]) keeps
# uvloop / httptools / watchfiles out of the bundle — we explicitly run
# uvicorn with loop="asyncio" so uvloop is dead weight inside the .app
# / .exe.
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
# FSEvents-backed file watcher on macOS, ReadDirectoryChangesW on Windows.
# Both backends ship inside watchdog and are picked at runtime.
watchdog==5.0.3
